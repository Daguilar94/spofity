<div class="container">
  <div class="row well">
    <div class="col-md-8">
      <h1><%=@artist.name%></h1>
    </div>
    <img src='<%=@artist.image_url%>' alt="Artist photo" width="300" height="300"><br>
  </div>
  <div class="row">
    <h3>Albums (<%=@albums.length%>)</h3>
  </div>
<% @albums.each do |album| %>
  <div class="frame col-md-3">
    <img src='<%=album.image_url%>' alt="Album photo" width="250" height="250"><br>
    <%=album.name%><br>
    <%=link_to "Ver más", artist_albums_path(@artist)%>
  </div>
<%end%>
</div>
